package exercise;

import java.awt.BorderLayout;
import java.awt.FlowLayout;
import java.awt.GridLayout;

/*
1. 자신이 원하는 위치에 버튼을 지정할 수 있다. ex) add(panel,BorderLayout.CENTER);
2. 프레임 BorderLayout / 패널 FlowLayout / 애플릿 FlowLayout
3. p1.setLayout(new GridLayout(0, 9)); 
4. 아래에 작성 
p.setLayout(null) or p.setLayout(new FlowLayout());
for(int i=0; i<30; i++){
labels[i] = new JLabel(""+i);
int x = (int) (500 * Math.random());
int y = (int) (500 * Math.random());
label[i].setForeground(Color.MAGENTA);
label[i].setLocation)(500,300);
label[i].setSize(300,200);
p.add(labels[i]);
 
 
 
 
 
 
  
  
  
 */


